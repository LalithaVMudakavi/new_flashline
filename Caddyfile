{
    frankenphp
}

:80 {
    root * /app/public

    encode gzip zstd

    # Serve existing static files directly
    @static {
        file
    }

    handle @static {
        file_server
    }

    # Everything else goes to Laravel
    handle {
        php_server
    }
}